{
	"name": "serverless-template",
	"version": "1.0.0",
	"description": "A boilerplate system design pattern to get started with Serverless",
	"scripts": {
		"sync": "cp .gitignore build/ && cp package.json build/ && cp serverless.yml build/",
		"deploy": "tsc && npm run sync && cd build && serverless deploy && cd ../ && rm -R build",
		"remove": "cd build && serverless remove",
		"test-local": "ts-node ./test/Local.ts",
		"test-release": "ts-node ./test/Release.ts"
	},
	"keywords": [
		"serverless",
		"nodejs",
		"ts"
	],
	"author": "Nicheal Pham",
	"license": "MIT",
	"dependencies": {
		"csv-parse": "^2.5.0",
		"rimraf": "^2.6.2",
		"zlib": "^1.0.5"
	},
	"devDependencies": {
		"@types/node": "^10.5.2",
		"serverless-plugin-typescript": "^1.1.5",
		"typescript": "^2.9.2",
		"typescript-eslint-parser": "^16.0.1"
	}
}
